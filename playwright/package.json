{
  "name": "internal-e2e",
  "private": true,
  "description": "Playwright e2e suite for the Systemprompt Internal gateway's browser surfaces (admin login, catalog, device-link). Needs a running stack: `just start`, then `just e2e`.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
