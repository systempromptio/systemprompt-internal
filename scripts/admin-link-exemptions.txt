# Admin hrefs that intentionally do not map to a `.route(...)` SSR literal:
# redirect stubs, aliases, or targets served by other means. One path per line,
# `#` comments. Never list a genuinely dead link here — fix the href or add the
# route instead.
