# Files exempt from the `///` placement rule in lint-inline-comments.sh.
# One path per line. Reserve this for files where rustdoc is *load-bearing*
# rather than decorative — never to mute a fixable violation.

# schemars-derived MCP tool schema: `///` on a `JsonSchema` field becomes the
# tool parameter description the model reads. Converting it to `// Why:` empties
# the description from the generated schema, so the placement rule cannot apply.
extensions/mcp/systemprompt/src/tools.rs
