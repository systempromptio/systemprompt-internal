id: systemprompt_setup
name: "Systemprompt Setup"
description: "Start here to set up systemprompt.io in your workspace: routes to the right host-specific setup skill (Claude Cowork dashboards, Codex CLI gateway wiring) and checks the Odoo connection"
# Retired: host targeting on the two real setup skills means each host now
# sees exactly one — the router this skill provided is redundant and only
# cluttered the picker with a third near-identical entry.
enabled: false
file: SKILL.md
tags:
  - setup
  - onboarding
  - getting-started
category: setup
display_category: "Brand & Workspace"
