# Blog/content ingestion config — read by the web jobs extension
# (extensions/web/jobs ingestion job via BlogConfigValidated::load_from_env_or_none).
# Relative paths resolve against this file's directory (services/config/).

base_url: "https://astounddigital.com"
enable_link_tracking: true

content_sources:
  - source_id: blog
    category_id: blog
    path: ../content/blog
    allowed_content_types:
      - blog
    enabled: true
    override_existing: true
  - source_id: documentation
    category_id: documentation
    path: ../content/documentation
    allowed_content_types:
      - guide
      - reference
      - tutorial
      - docs
      - feature
    enabled: true
    override_existing: true
